# 计算模块接口设计

## 1. 模块划分

```text
main.py
└── plagiarism_checker.py
    ├── normalize_text
    ├── build_ngram_frequency
    ├── cosine_similarity
    ├── calculate_similarity
    └── calculate_similarity_from_files
```

`main.py` 只负责命令行参数校验、调用流程和答案文件输出；`plagiarism_checker.py` 负责全部计算逻辑和可预期异常。模块之间通过字符串、`Counter[str]` 和浮点数传递数据，职责清晰，便于单元测试。

## 2. 核心算法

1. 读取两个 UTF-8 文件，支持并自动去除 UTF-8 BOM。
2. 用正则表达式删除所有空白字符，保留中文、英文、数字和标点。
3. 使用长度为 3 的滑动窗口生成字符级 N-gram。例如 `今天是星期天` 会生成 `今天是`、`天是星`、`是星期`、`星期天`。
4. 统计 N-gram 频次，得到稀疏词频向量。
5. 用余弦公式计算相似度，并将结果限制在 `[0, 1]`，CLI 最终格式化为两位小数。

对于完全相同的非空文本，程序直接返回 `1.0`，因此长度小于 3 的相同短文本也能得到直观结果；不同的短文本没有公共 3-gram，返回 `0.0`。

## 3. 正确性与复杂度

设去空白后的文本长度为 `n` 和 `m`。N-gram 统计分别为 `O(n)`、`O(m)`；余弦点积遍历较小的稀疏向量，空间复杂度为 `O(n + m)`。程序不使用网络，也只读两个输入文件、写一个输出文件。

## 4. 主流程

```text
命令行参数
    ↓
参数数量/空字符串校验 ──失败──> 用法提示，退出码 2
    ↓
读取原文和抄袭版 ───────失败──> 错误提示，退出码 1
    ↓
去空白 → 3-gram 词频 → 余弦相似度
    ↓
写入答案文件（%.2f）
```

## 5. 异常设计

| 异常 | 触发场景 | 设计目标 |
|---|---|---|
| `ArgumentError` | 非字符串文本、非法 N-gram 大小、非法路径类型 | 尽早指出调用契约错误 |
| `FileOperationError` | 输入文件不存在、无权限或编码错误 | 将底层文件异常转换成可读错误 |
| `EmptyDocumentError` | 任一输入只含空白 | 避免零向量导致无意义结果 |

命令行入口捕获上述异常和输出文件的 `OSError`，保证错误场景不会出现未处理异常退出。
